steps:

  - id: 'Build'
    name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/milestone-auto-supplies/milestone-website:latest',
      '.'
    ]
    env:
      - "DOCKER_BUILDKIT=1"
